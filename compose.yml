# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: zach
      POSTGRES_USER: zach
      POSTGRES_PASSWORD: zach
    ports:
      - "5400:5432"